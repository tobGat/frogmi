name: Deploy FrogMi

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Node.js einrichten
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Frontend bauen
        working-directory: client
        run: npm ci && npm run build

      - name: SSH einrichten
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          echo "${{ secrets.SSH_KNOWN_HOSTS }}" > ~/.ssh/known_hosts

      - name: Frontend deployen
        run: |
          ssh root@46.225.168.25 'mkdir -p /var/www/frogmi'
          scp -r client/dist/* root@46.225.168.25:/var/www/frogmi/

      - name: Backend deployen & neustarten
        run: |
          ssh root@46.225.168.25 'mkdir -p /opt/apps/frogmi'
          scp server/index.js server/gameLogic.js server/package.json root@46.225.168.25:/opt/apps/frogmi/
          ssh root@46.225.168.25 'cd /opt/apps/frogmi && npm install --production && (pm2 restart frogmi || PORT=3005 pm2 start index.js --name frogmi) && pm2 save'
